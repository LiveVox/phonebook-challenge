<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Contact">
    <select id="selectAll" resultMap="result">
        SELECT ID, NAME, LASTNAME, PHONE FROM CONTACTS;
    </select>

    <select id="findContact" resultMap="result">
        SELECT ID, NAME, LASTNAME, PHONE FROM CONTACTS
        WHERE
        <foreach item="item" collection="list" separator="OR">
            NAME LIKE #{item} OR LASTNAME LIKE #{item}
        </foreach>
        ;
    </select>

    <select id="findSpecific" resultMap="result" parameterType="co.com.liveBox.datamodel.vo.Contact">
        SELECT ID, NAME, LASTNAME, PHONE FROM CONTACTS
        WHERE NAME = #{name} AND LASTNAME = #{lastname} AND PHONE = #{phone};
    </select>

    <insert id="addContact" parameterType="co.com.liveBox.datamodel.vo.Contact">
        INSERT INTO CONTACTS (ID, NAME, LASTNAME, PHONE)
        VALUES (#{id, typeHandler=co.com.liveBox.datamodel.typehandlers.UUIDTypeHandler, javaType=java.util.UUID, jdbcType=VARCHAR},#{name},#{lastname},#{phone})
    </insert>

    <resultMap id="result" type="co.com.liveBox.datamodel.vo.Contact">
        <result property="id" column="ID" typeHandler="co.com.liveBox.datamodel.typehandlers.UUIDTypeHandler"/>
        <result property="name" column="NAME"/>
        <result property="lastname" column="LASTNAME"/>
        <result property="phone" column="PHONE"/>
    </resultMap>
</mapper>
